/target
**/*.rs.bk
.DS_Store

# Frontend build outputs
/ui/node_modules
/ui/dist

# Marketing site
/site/node_modules
/site/dist
/site/.astro

# Tauri generated schemas
/app/gen

# Account data, not source. The provisioning profile carries the Team ID and the certificate
# chain it was issued against; scripts/appstore.sh reads it from here rather than from the repo.
/app/MacAppStore.provisionprofile

# Intermediate icon directory; the .icns and .png files it produces are committed.
/app/icons/icon.iconset
